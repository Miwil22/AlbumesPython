# Memoria del Proyecto Álbumes

## 1. Integración de Jinja2 y API
Se han añadido plantillas HTML utilizando el motor **Jinja2** para visualizar los datos de la API. La estructura del proyecto separa la lógica de negocio (routers) de la presentación (templates).

**Prueba de funcionamiento:**
La siguiente captura muestra la página de inicio renderizada dinámicamente junto con el comando
que demuestra que la ejecución es correcta:

![App](src/static/img/punto1.png)


## 2. Base de datos en local
En esta fase se ha implementado la conexión a una base de datos real para que la información no se pierda al reiniciar el servidor.

![App](src/static/img/punto2.png)